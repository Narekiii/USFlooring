# Contact form configuration

The contact form API requires `WEB3FORMS_ACCESS_KEY`. Configure this variable separately in both the Vercel Preview and Production environments; do not commit the real key.
